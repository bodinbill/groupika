# Groupika React Web Client

## Run
You can run this via npm:
```sh
$ npm start
```

## Build and Deploy to Firebase
Firebase hosting only work with static content that have to pack the project, following this step.

```sh
$ npm build
$ firebase deploy --project tripwithfriends-71e07
```
